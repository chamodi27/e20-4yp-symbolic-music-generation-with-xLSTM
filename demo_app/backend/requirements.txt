# Flask API additional dependencies
# All other packages (torch, xlstm, safetensors, etc.) are expected to be
# already present in the base conda/pip environment (see environment.yml).
#
# Pinned versions match the tested midi-decode-test environment.
# Do NOT change numpy, miditoolkit, or midiprocessor versions without testing.

# Web server
flask>=3.0
flask-cors>=4.0

# Token generation (xLSTM)
xlstm==2.0.5
safetensors==0.7.0
transformers==4.44.0
omegaconf==2.3.0
dacite==1.9.2

# MIDI decoding — pinned versions (do not change without testing)
# midiprocessor is not on PyPI — must install from GitHub source
git+https://github.com/btyu/MidiProcessor.git
miditoolkit==0.1.16
mido==1.3.3
pretty-midi==0.2.11
numpy==1.23.5
scipy==1.10.1
